
static_library("YFramework") {
  sources = [
    "YFramework.h",
    "YFramework.cpp",
  ]

  if (is_win) {
    sources += [
      "PlatformHandle_Win.h",
      "PlatformHandle_Win.cpp",
    ]
  }

  deps = [
    "//YEngine/YCore",
  ]
}
