
#####
# Everything includes from the root #
#####
config("default_include_dirs") {
  include_dirs = [
    "//"
  ]
}

#####
# Standard Defines for each configuration
#####
config("debug") {
  defines = [
    "_DEBUG",
    "DEBUG",
  ]
}

config("release") {
  defines = [
    "RELEASE"
  ]
}

config("gold") {
  defines = [
    "GOLD",
  ]
}

config("platinum") {
  defines = [
    "PLATINUM",
  ]
}
