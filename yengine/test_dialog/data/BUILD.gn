import("//ytools/tools.gni")

compile_shader("fullscreen_shdr") {
  shader_file = "shaders/full_screen.shdr.json"
  output_file = "${root_build_dir}/data/full_screen.shdr.bin"
  inputs = [ "shaders/full_screen.hlsl" ]
}
