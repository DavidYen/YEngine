import("//build/build_asset.gni")

compile_asset_database("test_dialog_asset_database") {
  sources = [
    "${root_build_dir}/data/full_screen.shdr",
  ]
  output = "${root_build_dir}/data/test_dialog_assets.json"

  deps = [
    "shaders:fullscreen_shdr",
  ]
}
