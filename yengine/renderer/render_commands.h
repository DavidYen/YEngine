#ifndef YENGINE_RENDERER_RENDER_COMMANDS_H
#define YENGINE_RENDERER_RENDER_COMMANDS_H

#include "yengine/render_device/render_device.h"
#include "yengine/renderer/renderer.h"

namespace yengine { namespace renderer {

class RenderCommands {
 public:
  RenderCommands();
  ~RenderCommands();

  void AddCommand(uint8_t render_pass, ViewPortID viewport, 

 private:
  
};

}} // namespace yengine { namespace renderer {

#endif // YENGINE_RENDERER_RENDER_COMMANDS_H
